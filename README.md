# Polymarket Grapher

Визуализируйте цены ставок на Polymarket вместе с ценами криптовалют.

## Возможности

- 📊 Визуализация истории цен ставок YES/NO для событий Polymarket
- 🪙 Сравнение с ценами криптовалют (BTC, ETH, SOL) за тот же период
- 📈 Отображение суммы выбранных ставок на графике
- 🎨 Современный темный интерфейс с Material UI

## Установка

```bash
npm install
```

## Запуск (разработка)

```bash
npm run dev
```

Приложение будет доступно по адресу http://localhost:5173

В режиме разработки API запросы проксируются через Vite dev server.

## Использование

1. **Первый экран**: Введите URL события Polymarket (например, `https://polymarket.com/event/bitcoin-above-on-december-1`) и выберите криптовалюту для сравнения (опционально)

2. **Второй экран**: Выберите интересующие вас рынки с помощью чекбоксов YES/NO. График автоматически обновится, показывая историю цен выбранных ставок

---

## Деплой в Production

### Шаг 1: Деплой Cloudflare Worker

Для обхода CORS ограничений в production необходимо задеплоить Cloudflare Worker.

#### 1.1 Установите Wrangler CLI

```bash
npm install -g wrangler
```

#### 1.2 Авторизуйтесь в Cloudflare

```bash
wrangler login
```

#### 1.3 Задеплойте воркер

```bash
cd worker
wrangler deploy
```

После успешного деплоя вы получите URL вида:
```
https://polymarket-proxy.<your-subdomain>.workers.dev
```

Сохраните этот URL — он понадобится на следующем шаге.

> **Примечание:** Подробная инструкция по воркеру находится в [worker/README.md](worker/README.md)

### Шаг 2: Деплой на GitHub Pages

#### 2.1 Создайте репозиторий на GitHub

Если репозиторий ещё не создан, создайте его и загрузите код:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/<username>/polymarket-grapher.git
git push -u origin main
```

#### 2.2 Настройте GitHub Actions secret

1. Перейдите в **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **New repository secret**
3. Добавьте секрет:
   - **Name:** `VITE_WORKER_URL`
   - **Value:** URL вашего Cloudflare Worker (например, `https://polymarket-proxy.your-subdomain.workers.dev`)

#### 2.3 Включите GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите **GitHub Actions**

#### 2.4 Запустите деплой

Деплой запускается автоматически при push в ветку `main`.

Для ручного запуска:
1. Перейдите на вкладку **Actions**
2. Выберите workflow **Deploy to GitHub Pages**
3. Нажмите **Run workflow**

После успешного деплоя приложение будет доступно по адресу:
```
https://<username>.github.io/polymarket-grapher/
```

### Настройка для другого имени репозитория

Если ваш репозиторий называется иначе, измените `base` в `vite.config.ts`:

```typescript
base: process.env.GITHUB_ACTIONS ? '/<your-repo-name>/' : '/',
```

### Деплой на кастомный домен

Если вы хотите использовать свой домен:

1. В настройках GitHub Pages укажите кастомный домен
2. Измените `base` в `vite.config.ts` на `'/'`
3. Добавьте файл `CNAME` в папку `public/` с вашим доменом

---

## Технологии

- React 19
- TypeScript
- Vite
- Material UI (MUI)
- Recharts
- Axios
- Cloudflare Workers

## API

Приложение использует следующие API:
- [Polymarket Gamma API](https://docs.polymarket.com/api-reference/events/get-event-by-slug) - для получения данных о событиях
- [Polymarket CLOB API](https://docs.polymarket.com/api-reference/pricing/get-price-history-for-a-traded-token) - для истории цен ставок
- [Binance API](https://binance-docs.github.io/apidocs/spot/en/) - для цен криптовалют

## Структура проекта

```
polymarket-grapher/
├── src/
│   ├── api/
│   │   ├── config.ts      # Конфигурация API endpoints (dev/prod)
│   │   ├── polymarket.ts  # API Polymarket
│   │   └── binance.ts     # API Binance
│   ├── components/
│   └── ...
├── worker/
│   ├── index.js           # Cloudflare Worker
│   ├── wrangler.toml      # Конфигурация Wrangler
│   └── README.md          # Инструкция по деплою воркера
├── .github/
│   └── workflows/
│       └── deploy.yml     # GitHub Actions workflow
└── vite.config.ts
```

## Лимиты бесплатных планов

### Cloudflare Workers (бесплатно)
- 100,000 запросов в день
- Неограниченное количество воркеров

### GitHub Actions (бесплатно для публичных репозиториев)
- 2,000 минут в месяц для приватных репозиториев
- Неограниченно для публичных репозиториев

### GitHub Pages (бесплатно)
- 1 GB хранилища
- 100 GB трафика в месяц

## Лицензия

MIT
